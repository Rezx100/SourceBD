# Env / secrets
.env
.env.*
!.env.example

# Agent debugging file (may contain leaked tokens from logs)
context/current-issues.md

# Node
node_modules
.pnp/
.pnp.js

# Next.js
.next/
out/
build/
dist/
tsconfig.tsbuildinfo

# Caches
.cache/
.turbo/
.eslintcache
.parcel-cache

# Python ETL
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Raw scraped/downloaded source data (large, re-fetchable)
etl/raw/
data/raw/
data/parsed/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Supabase
supabase/.branches
supabase/.temp

# Scratch / debug artefacts (kept locally, never committed)
ops/_*
ops/screenshots/
# Operator credential files kept locally. `ops/_*` does not match a leading dot,
# so incoming webhook URLs (which are credentials) need naming explicitly.
ops/.slack-webhooks.psd1
ops/*.secrets.*

# Legacy deploy-quick pack artifacts (laptop only)
_quick_*.tar.gz
_quick_*.list
_quick_*.sh
_preflight.*

# VPS deploy metadata (written on server at /opt/sourcebd/.deploy/)
.deploy/

# Local agent/editor config
.claude/
.cursor/settings.local.json

# Accidental DB dumps
*.sql.gz
*.dump
*.bak

.devlog.txt
.devlog-review.txt
.buildlog.txt
# Unrelated browser-saved help page
API Access _ OneProvider.htm
API Access _ OneProvider_files/

# compiled node:test output (npm test)
.tests-build/

# Screen recordings and session exports at the repo root (never source)
/*.webm
/Claude outputs/

# Gallery evidence harness output (scripts/gallery/regen.sh)
/_gallery-out/
/.render-build/
